<div class="page-container">
  <%= link_to "← Back to Home", root_path, class: "back-link" %>
  
  <div class="page-content">
    <h1><%= @page.title %></h1>
    <div class="content">
      <%= raw @page.content %>
    </div>
  </div>
</div>
